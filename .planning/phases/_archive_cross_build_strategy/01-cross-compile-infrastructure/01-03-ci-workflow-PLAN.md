---
phase: 01-cross-compile-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - project/Commons.scala
  - .github/workflows/ci.yml
autonomous: false
requirements: [INFRA-07, WORKFLOW-01, WORKFLOW-02, WORKFLOW-03, WORKFLOW-04, WORKFLOW-05]
must_haves:
  truths:
    - "`githubWorkflowJavaVersions` in Commons.scala lists ONLY `JavaSpec.temurin(\"17\")` — no 21, no 25"
    - "`githubWorkflowBuild` in Commons.scala defines a single `WorkflowStep.Sbt` invoking the 5 gate commands in this exact order: +jvm/test, +jvm2/test, +js/test, ++2.13 mimaReportBinaryIssues, scalafmtCheckAll"
    - "The two `githubWorkflowAddedJobs += WorkflowJob(id = \"mima\", ...)` and `githubWorkflowAddedJobs += WorkflowJob(id = \"scalafmt\", ...)` blocks are REMOVED from Commons.scala"
    - "`.github/workflows/ci.yml` regenerated by `sbt githubWorkflowGenerate` and committed in the same commit as the Commons.scala edits"
    - "`sbt githubWorkflowCheck` exits 0 (regenerated workflow matches checked-in file)"
    - "All 5 gate commands pass locally on Java 17"
    - "User explicitly acks before `git push origin 01-cross-compile-infra` AND before `gh pr create`"
  artifacts:
    - path: project/Commons.scala
      provides: "CI workflow keys — Java 17 only, single 5-command build step"
      contains: 'githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17"))'
    - path: .github/workflows/ci.yml
      provides: "Generated GitHub Actions workflow"
      contains: "+jvm/test"
  key_links:
    - from: "githubWorkflowBuild sbt key"
      to: "ci.yml Run CI gate step"
      via: "sbt githubWorkflowGenerate"
      pattern: "Run CI gate"
    - from: "githubWorkflowJavaVersions"
      to: "ci.yml matrix.java"
      via: "sbt-github-actions matrix derivation"
      pattern: "java:.*17"
---

<objective>
Configure `sbt-github-actions` keys in `project/Commons.scala` so the regenerated `.github/workflows/ci.yml` runs exactly the 5 gate commands (REQ INFRA-07) on Java 17 only. Fold the previously-separate `mima` and `scalafmt` added jobs into the single matrix per user directive. Regenerate ci.yml, run the full 5-gate suite locally, then present the branch for user ack before push, and again before PR open (REQ WORKFLOW-01..05).

Purpose: Closes the CI surface area of Phase 1 and establishes the per-PR workflow contract every subsequent migration PR follows.

Output: Updated `project/Commons.scala` (CI keys), regenerated `.github/workflows/ci.yml`, branch `01-cross-compile-infra` pushed to `origin` after user ack, PR opened against `AVSystem/scala-commons:scala-3` after second user ack.
</objective>

<execution_context>
@/Users/bkozak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bkozak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-cross-compile-infrastructure/01-CONTEXT.md
@.planning/phases/01-cross-compile-infrastructure/01-RESEARCH.md
@.planning/phases/01-cross-compile-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-cross-compile-infrastructure/01-02-SUMMARY.md
</context>

<interfaces>
Relevant sbt-github-actions 0.30.0 keys (from sbtghactions.GenerativePlugin.autoImport):

```
val githubWorkflowJavaVersions: SettingKey[Seq[JavaSpec]]
val githubWorkflowBuild: SettingKey[Seq[WorkflowStep]]
val githubWorkflowAddedJobs: SettingKey[Seq[WorkflowJob]]
val githubWorkflowEnv: SettingKey[Map[String, String]]
val githubWorkflowBuildMatrixFailFast: SettingKey[Option[Boolean]]
val githubWorkflowBuildPreamble: SettingKey[Seq[WorkflowStep]]
val githubWorkflowTargetTags: SettingKey[Seq[String]]
val githubWorkflowArtifactUpload: SettingKey[Boolean]
val githubWorkflowPublishTargetBranches: SettingKey[Seq[RefPredicate]]
val githubWorkflowPublish: SettingKey[Seq[WorkflowStep]]
```

Generate / check commands:
- `sbt githubWorkflowGenerate` — writes/overwrites `.github/workflows/ci.yml`
- `sbt githubWorkflowCheck` — exits non-zero if ci.yml drifts from sbt config

API constructors used:
- `WorkflowStep.Sbt(commands: List[String], name: Option[String] = None, ...)`
- `JavaSpec.temurin(version: String): JavaSpec`

Upstream/scala-3 baseline CI keys (current state in Commons.scala buildSettings; preserved through Plan 02 since Plan 02 did not touch these):

- `githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17"), JavaSpec.temurin("21"), JavaSpec.temurin("25"))` — must be narrowed to Java 17 only.
- `githubWorkflowBuildMatrixFailFast := Some(false)` — preserve.
- Two `githubWorkflowAddedJobs += WorkflowJob(...)` blocks (id "mima" and id "scalafmt", both pinned to Java 21) — must be DELETED.
- `githubWorkflowBuildPreamble ++= Seq(setup-node, setup-MongoDB)` — preserve (still needed for tests).
- `githubWorkflowPublishTargetBranches`, `githubWorkflowPublish`, `githubWorkflowEnv`, `githubWorkflowTargetTags`, `githubWorkflowArtifactUpload` — preserve.

The default `githubWorkflowBuild` (if NOT overridden) is `Seq(WorkflowStep.Sbt(List("test"), ...))`. REQ INFRA-07 mandates overriding it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Edit Commons.scala CI keys — Java 17 only, single 5-gate build step, drop mima/scalafmt added jobs</name>
  <files>project/Commons.scala</files>
  <read_first>
    - project/Commons.scala (post-Plan-02 state — full buildSettings block)
    - .planning/phases/01-cross-compile-infrastructure/01-RESEARCH.md §"Pattern 5: sbt-github-actions custom build step"
    - .planning/phases/01-cross-compile-infrastructure/01-CONTEXT.md §decisions ("CI workflow")
  </read_first>
  <action>
    Edit `/Users/bkozak/IdeaProjects/scala-commons3/project/Commons.scala` inside `override def buildSettings: Seq[Def.Setting[_]] = Seq(...)`. All edits below are surgical.

    (a) Narrow Java versions. Replace this line verbatim:

        githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17"), JavaSpec.temurin("21"), JavaSpec.temurin("25")),

    with exactly:

        githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17")),

    Rationale: REQ INFRA-07 says "Java 17 only"; Java 21/25 deferred to v2 CI-01 per CONTEXT.

    (b) Add the 5-gate build step override. AFTER the line `githubWorkflowBuildMatrixFailFast := Some(false),` and BEFORE the existing `githubWorkflowAddedJobs += WorkflowJob(id = "mima", ...)` block, insert (indentation: four spaces matching surrounding settings):

        githubWorkflowBuild := Seq(
          WorkflowStep.Sbt(
            List(
              "+jvm/test",
              "+jvm2/test",
              "+js/test",
              "++2.13 mimaReportBinaryIssues",
              "scalafmtCheckAll",
            ),
            name = Some("Run CI gate"),
          ),
        ),

    Order rationale: `+jvm/test` first (longest, fail-fast); `+jvm2/test` next (jetty 2.13 only); `+js/test` after; `++2.13 mimaReportBinaryIssues` (pinned to 2.13 — no Scala 3 MiMa baseline yet); `scalafmtCheckAll` last.

    (c) DELETE both `githubWorkflowAddedJobs += WorkflowJob(...)` blocks entirely:
       - The block with `id = "mima"` (Binary Compatibility Check) — full block from `githubWorkflowAddedJobs += WorkflowJob(` through its terminating `),`.
       - The block with `id = "scalafmt"` (Scalafmt Check) — full block similarly.

    Both blocks span multiple lines; delete from the start of `githubWorkflowAddedJobs += WorkflowJob(` down to and including the matching `),` (the comma after the closing paren). Preserve clean trailing-comma layout: the line above and the line below should chain naturally.

    Rationale (user directive): "fold mima/scalafmt addedJobs into single matrix per literal REQ INFRA-07 reading". The 5-command build step now runs `mimaReportBinaryIssues` and `scalafmtCheckAll` inline.

    (d) Leave ALL other `githubWorkflow*` settings untouched: `githubWorkflowTargetTags`, `githubWorkflowArtifactUpload`, `githubWorkflowEnv`, `githubWorkflowBuildPreamble` (setup-node + setup-MongoDB — these run before every matrix job and are still needed for integration tests in later phases; harmless for Phase 1 where nothing requires them), `githubWorkflowPublishTargetBranches`, `githubWorkflowPublish`.

    (e) Leave ALL non-CI settings untouched (organization, scmInfo, developers, scalaVersion, crossScalaVersions, etc.).

    Sanity check after edit, from project root:

        cd /Users/bkozak/IdeaProjects/scala-commons3
        sbt -batch reload

    MUST exit 0. If reload fails, the most likely cause is a comma-layout mistake from the multi-line delete — re-read the diff and fix before proceeding.

    DO NOT COMMIT YET. Task 2 regenerates `ci.yml` and Task 3 runs the full gate suite; commit happens in Task 3.
  </action>
  <verify>
    <automated>cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; grep -Fxq '    githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17")),' project/Commons.scala &amp;&amp; ! grep -Fq 'JavaSpec.temurin("21")' project/Commons.scala &amp;&amp; ! grep -Fq 'JavaSpec.temurin("25")' project/Commons.scala &amp;&amp; grep -Fq 'name = Some("Run CI gate")' project/Commons.scala &amp;&amp; ! grep -Fq 'id = "mima"' project/Commons.scala &amp;&amp; ! grep -Fq 'id = "scalafmt"' project/Commons.scala &amp;&amp; sbt -batch reload</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fxq '    githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17")),' project/Commons.scala` exits 0.
    - `grep -Fq 'JavaSpec.temurin("21")' project/Commons.scala` exits 1 (Java 21 removed).
    - `grep -Fq 'JavaSpec.temurin("25")' project/Commons.scala` exits 1 (Java 25 removed).
    - `grep -Fq 'name = Some("Run CI gate")' project/Commons.scala` exits 0 (new build step present).
    - All 5 gate commands present in the build step, in order: `grep -A8 'Run CI gate' project/Commons.scala` is NOT empty AND `sed -n '/Run CI gate/,/^$/p' project/Commons.scala` (or just `grep` for each literal) finds each of `"+jvm/test"`, `"+jvm2/test"`, `"+js/test"`, `"++2.13 mimaReportBinaryIssues"`, `"scalafmtCheckAll"`.
    - `grep -Fq 'id = "mima"' project/Commons.scala` exits 1 (mima added job removed).
    - `grep -Fq 'id = "scalafmt"' project/Commons.scala` exits 1 (scalafmt added job removed).
    - `grep -Fq 'githubWorkflowBuildPreamble' project/Commons.scala` exits 0 (preamble preserved).
    - `grep -Fq 'githubWorkflowPublish ' project/Commons.scala` exits 0 (publish workflow preserved).
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch reload` exits 0.
  </acceptance_criteria>
  <done>`Commons.scala` CI keys narrowed to Java 17 + single 5-gate build step; mima/scalafmt added jobs deleted; preamble + publish preserved; `sbt reload` succeeds. Not yet committed.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate .github/workflows/ci.yml via sbt githubWorkflowGenerate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - project/Commons.scala (post-Task-1 state — to know what the regenerated yml should reflect)
    - .github/workflows/ci.yml (existing upstream version — for awareness of what changes; do NOT hand-edit)
  </read_first>
  <action>
    From project root, run:

        cd /Users/bkozak/IdeaProjects/scala-commons3
        sbt -batch githubWorkflowGenerate

    This OVERWRITES `.github/workflows/ci.yml` based on the current sbt keys. Do NOT hand-edit the resulting file — sbt-github-actions ships a "Check that workflows are up to date" step that fails the build on drift. The file header warns: "you shouldn't edit this file by hand!"

    After regeneration, verify the file matches the sbt config:

        sbt -batch githubWorkflowCheck

    MUST exit 0. If it fails, re-run `githubWorkflowGenerate` (the prior invocation may have failed mid-write).

    Spot-check the generated `ci.yml`:
    - It MUST contain the literal string `Run CI gate` (the name of the WorkflowStep.Sbt).
    - It MUST contain each of the 5 gate commands as separate `++` / `+` invocations within the run script of the build job (sbt-github-actions collapses a single WorkflowStep.Sbt into one `sbt ...` shell line: `sbt '++${{ matrix.scala }}' '+jvm/test' '+jvm2/test' '+js/test' '++2.13 mimaReportBinaryIssues' scalafmtCheckAll` or similar — the literal substrings `+jvm/test`, `+jvm2/test`, `+js/test`, `++2.13 mimaReportBinaryIssues`, `scalafmtCheckAll` MUST all be present).
    - It MUST contain `java: [temurin@17]` (or equivalent — sbt-github-actions formats temurin Java 17 as `temurin@17` in the matrix). It MUST NOT contain `temurin@21` or `temurin@25`.
    - It MUST NOT contain a job with `name: Binary Compatibility Check` or `name: Scalafmt Check` (those were the added jobs).
    - It MUST contain the `Setup Node.js` and `Setup MongoDB` steps from the preserved preamble.

    DO NOT commit yet — Task 3 runs the full local gate and bundles everything into a single commit.
  </action>
  <verify>
    <automated>cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch githubWorkflowCheck &amp;&amp; grep -Fq 'Run CI gate' .github/workflows/ci.yml &amp;&amp; grep -Fq '+jvm/test' .github/workflows/ci.yml &amp;&amp; grep -Fq '+jvm2/test' .github/workflows/ci.yml &amp;&amp; grep -Fq '+js/test' .github/workflows/ci.yml &amp;&amp; grep -Fq '++2.13 mimaReportBinaryIssues' .github/workflows/ci.yml &amp;&amp; grep -Fq 'scalafmtCheckAll' .github/workflows/ci.yml &amp;&amp; grep -Fq 'temurin@17' .github/workflows/ci.yml &amp;&amp; ! grep -Fq 'temurin@21' .github/workflows/ci.yml &amp;&amp; ! grep -Fq 'temurin@25' .github/workflows/ci.yml &amp;&amp; ! grep -Fq 'Binary Compatibility Check' .github/workflows/ci.yml &amp;&amp; ! grep -Fq 'Scalafmt Check' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch githubWorkflowCheck` exits 0 (no drift between sbt config and generated ci.yml).
    - `grep -Fq 'Run CI gate' .github/workflows/ci.yml` exits 0.
    - All 5 gate commands present: `grep -Fq '+jvm/test'`, `grep -Fq '+jvm2/test'`, `grep -Fq '+js/test'`, `grep -Fq '++2.13 mimaReportBinaryIssues'`, `grep -Fq 'scalafmtCheckAll'` — each exits 0 against `.github/workflows/ci.yml`.
    - `grep -Fq 'temurin@17' .github/workflows/ci.yml` exits 0.
    - `grep -Fq 'temurin@21' .github/workflows/ci.yml` exits 1 (no Java 21).
    - `grep -Fq 'temurin@25' .github/workflows/ci.yml` exits 1 (no Java 25).
    - `grep -Fq 'Binary Compatibility Check' .github/workflows/ci.yml` exits 1 (no separate mima job).
    - `grep -Fq 'Scalafmt Check' .github/workflows/ci.yml` exits 1 (no separate scalafmt job).
    - `grep -Fq 'Setup Node.js' .github/workflows/ci.yml` exits 0 (preamble preserved).
    - `grep -Fq 'Setup MongoDB' .github/workflows/ci.yml` exits 0 (preamble preserved).
  </acceptance_criteria>
  <done>`.github/workflows/ci.yml` regenerated by sbt-github-actions; `githubWorkflowCheck` clean; all 5 gate commands and `temurin@17` present; old added jobs absent.</done>
</task>

<task type="auto">
  <name>Task 3: Run full 5-gate suite locally + commit Commons.scala + ci.yml together</name>
  <files>project/Commons.scala, .github/workflows/ci.yml</files>
  <read_first>
    - project/Commons.scala (post-Task-1 state)
    - .github/workflows/ci.yml (post-Task-2 state)
  </read_first>
  <action>
    From project root, run the EXACT 5-command suite that CI will run (REQ INFRA-07 success criterion 5: "Clean local checkout reproduces green matrix"):

        cd /Users/bkozak/IdeaProjects/scala-commons3
        sbt -batch '+jvm/test' '+jvm2/test' '+js/test' '++2.13 mimaReportBinaryIssues' scalafmtCheckAll

    Expected outcomes:
    - `+jvm/test`: exits 0. Runs `jvm/test` for each `crossScalaVersions` entry on `jvm` (which is `Seq(scala3Version, scala2Version)`). On Scala 3, modules with no Scala 3 sources produce empty test classes — test pass with zero assertions, which is GREEN. On Scala 2.13, existing test suites run.
    - `+jvm2/test`: exits 0. Runs `jvm2/test` for `Seq(scala3Version, scala2Version)` BUT `jetty` has `Test/skip := true` on Scala 3, so the Scala 3 invocation is a no-op; Scala 2.13 invocation runs jetty tests.
    - `+js/test`: exits 0. Runs Scala.js tests on `core-js` and `mongo-js` for both versions; same empty-test-on-Scala-3 dynamic.
    - `++2.13 mimaReportBinaryIssues`: exits 0. Switches to Scala 2.13 and runs MiMa against the existing `previousCompatibleVersions` set. Since no source changes in Phase 1, existing `coreMimaFilters` and mongo filters already cover prior breakage — MiMa should be silent.
    - `scalafmtCheckAll`: exits 0 (already verified in Plan 01, but re-running confirms no drift introduced by Plan 02/03 edits to Commons.scala — Commons.scala is itself subject to scalafmt).

    If `scalafmtCheckAll` fails on `project/Commons.scala`, run `sbt scalafmtAll` to fix and re-run the suite — the Commons.scala edits in Plan 02/03 may have introduced formatting deviations. Do NOT bypass with `@nowarn` / `-Wconf` (REQ QUALITY-01).

    If any test fails on the SCALA 3 side due to actual source issues (not Phase 1's responsibility), surface to user via SUMMARY and STOP — this is a signal that the empty-jar assumption from CONTEXT does not hold for the current upstream/scala-3 tree, and Phase 2+ planning needs to account for it.

    Once all 5 commands exit 0, commit the Plan 03 diff (both `project/Commons.scala` CI-key changes and the regenerated `.github/workflows/ci.yml`) as a single commit. Stage explicitly to avoid `.planning/` leakage:

        cd /Users/bkozak/IdeaProjects/scala-commons3
        git add project/Commons.scala .github/workflows/ci.yml
        git status --porcelain    # MUST show only the two staged files; surface any extras to user
        git commit -m 'ci(workflow): single 5-gate matrix on Java 17 — fold mima/scalafmt into build step'

    Commit message rules: no GSD nomenclature (no `gsd`, no `phase N`, no `plan-phase`). Prefix `ci(workflow):` is allowed per CONTEXT specifics.

    Final verification on the branch:

        git log upstream/scala-3..HEAD --name-only | grep -c '^\.planning'    # MUST be 0
        git log upstream/scala-3..HEAD --format=%B | grep -iE 'gsd|phase [0-9]|plan-phase'    # MUST exit 1
        git log upstream/scala-3..HEAD --oneline | wc -l    # SHOULD be 4 (1 from Plan 01 task 2 + 1 from Plan 01 task 3 + 1 from Plan 02 + 1 from Plan 03)
  </action>
  <verify>
    <automated>cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch '+jvm/test' '+jvm2/test' '+js/test' '++2.13 mimaReportBinaryIssues' scalafmtCheckAll &amp;&amp; git log -1 --format=%s | grep -E '^ci\(workflow\):' &amp;&amp; ! git log -1 --format=%B | grep -iE 'gsd|phase [0-9]|plan-phase' &amp;&amp; ! git diff upstream/scala-3..HEAD --name-only | grep -q '^\.planning'</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch '+jvm/test'` exits 0.
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch '+jvm2/test'` exits 0.
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch '+js/test'` exits 0.
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch '++2.13 mimaReportBinaryIssues'` exits 0.
    - `cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; sbt -batch scalafmtCheckAll` exits 0.
    - Commit message starts with `ci(workflow):` — `git log -1 --format=%s | grep -E '^ci\(workflow\):'` exits 0.
    - Commit message has no GSD nomenclature — `git log -1 --format=%B | grep -iE 'gsd|phase [0-9]|plan-phase'` exits 1 (REQ WORKFLOW-04).
    - Commit touches ONLY `project/Commons.scala` and `.github/workflows/ci.yml`: `git show --stat HEAD --name-only --pretty= | sort -u` prints exactly those two paths (after stripping blank lines).
    - Across the whole branch: `git diff upstream/scala-3..HEAD --name-only | grep -c '^\.planning'` prints `0` (REQ WORKFLOW-05).
    - Across the whole branch: `git log upstream/scala-3..HEAD --format=%B | grep -iE 'gsd|phase [0-9]|plan-phase'` exits 1 (REQ WORKFLOW-04 over all commits).
    - The branch contains the 4 expected files in total: `git diff upstream/scala-3..HEAD --name-only | sort -u` lists exactly `.github/workflows/ci.yml`, `.scalafmt.conf`, `project/Commons.scala`, `project/plugins.sbt`.
  </acceptance_criteria>
  <done>All 5 gate commands pass locally on Java 17; Plan 03 changes committed as a single `ci(workflow):` commit; branch carries 4 commits total touching exactly 4 files; no `.planning/` paths, no GSD nomenclature in any commit message.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User ack before push to origin</name>
  <files>(none — gate)</files>
  <what-built>
    Branch `01-cross-compile-infra` on local repo `/Users/bkozak/IdeaProjects/scala-commons3` carries the complete Phase 1 cross-compile infrastructure slice on top of `upstream/scala-3`:
    - `project/plugins.sbt`: sbt-mima-plugin bumped to 1.1.5
    - `.scalafmt.conf`: dialect inverted (scala3 default + scala-2.13/ override to scala213source3)
    - `project/Commons.scala`: jvm/jvm2/js aggregates, per-module crossScalaVersions, scalaVersion=scala3Version, made dep on core (Scala 3 only), jetty skip block, Java 17 only, single 5-gate WorkflowStep.Sbt, mima/scalafmt added jobs removed
    - `.github/workflows/ci.yml`: regenerated by sbt-github-actions

    All 5 CI gate commands pass locally on Java 17. No `.planning/` paths in any commit. No GSD nomenclature in any commit message.
  </what-built>
  <how-to-verify>
    1. Inspect the branch diff:
       ```
       cd /Users/bkozak/IdeaProjects/scala-commons3
       git log upstream/scala-3..HEAD --oneline
       git diff upstream/scala-3..HEAD --stat
       git diff upstream/scala-3..HEAD -- .scalafmt.conf project/plugins.sbt project/Commons.scala
       git diff upstream/scala-3..HEAD -- .github/workflows/ci.yml | head -100
       ```
       Confirm: 4 files modified, expected diff content, no `.planning/` paths.

    2. Re-run the local 5-gate suite to confirm reproducibility:
       ```
       cd /Users/bkozak/IdeaProjects/scala-commons3
       sbt -batch '+jvm/test' '+jvm2/test' '+js/test' '++2.13 mimaReportBinaryIssues' scalafmtCheckAll
       ```
       MUST exit 0.

    3. (Optional, recommended for first PR per ROADMAP success criterion 5): clean clone reproduction.
       ```
       git clone /Users/bkozak/IdeaProjects/scala-commons3 /tmp/sc-phase1-verify
       cd /tmp/sc-phase1-verify && git checkout 01-cross-compile-infra
       sbt -batch '+jvm/test' '+jvm2/test' '+js/test' '++2.13 mimaReportBinaryIssues' scalafmtCheckAll
       ```
       MUST exit 0 (proves no local-only sbt cache shenanigans).

    4. When satisfied, ack the push. After your ack, Claude will run:
       ```
       cd /Users/bkozak/IdeaProjects/scala-commons3
       git push -u origin 01-cross-compile-infra
       ```
       Push target: your fork (`origin`). Push target is NOT `upstream` (REQ WORKFLOW-01..02: branch off upstream, push to your fork, PR targets `AVSystem/scala-commons:scala-3`).
  </how-to-verify>
    <action>Pause execution. Present the verification steps above to the user and wait for their reply. Do NOT push to origin until the user responds with `ack push`.</action>
  <verify><automated>echo 'checkpoint — requires human ack'</automated></verify>
  <done>User has replied with `ack push` (or amended-and-acked). Branch ready to push.</done>
  <resume-signal>Reply `ack push` to authorize `git push -u origin 01-cross-compile-infra`. Reply with issue list to block.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Push branch to origin (after Task 4 ack)</name>
  <files>(none — git plumbing)</files>
  <read_first>(none)</read_first>
  <action>
    Only run after Task 4 ack. From project root:

        cd /Users/bkozak/IdeaProjects/scala-commons3
        git push -u origin 01-cross-compile-infra

    Then wait for GitHub Actions to start on the pushed branch (typically &lt;30s). Poll status:

        gh run list --branch 01-cross-compile-infra --limit 5

    The CI run should appear with status `queued` then `in_progress`. Do NOT proceed to Task 6 until at least one workflow run completes (status `completed`, conclusion `success`). Use:

        gh run watch $(gh run list --branch 01-cross-compile-infra --limit 1 --json databaseId --jq '.[0].databaseId')

    If conclusion is `failure`:
    1. Surface the failed workflow URL via `gh run view --log-failed`.
    2. Diagnose: most likely causes are platform-specific MongoDB / Node setup flakiness, or a Scala-3-side source surprise that local Java 17 hides (unlikely for Phase 1 zero-source slice but possible).
    3. STOP and surface to user; do NOT open a PR on red CI.

    If conclusion is `success`, proceed to Task 6.

    Per REQ WORKFLOW-03: NO PR is opened in this task. PR creation requires a second user ack in Task 6.
  </action>
  <verify>
    <automated>cd /Users/bkozak/IdeaProjects/scala-commons3 &amp;&amp; git ls-remote --heads origin 01-cross-compile-infra | grep -q 01-cross-compile-infra &amp;&amp; gh run list --branch 01-cross-compile-infra --limit 1 --json conclusion --jq '.[0].conclusion' | grep -Fxq 'success'</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --heads origin 01-cross-compile-infra` prints a non-empty line (branch exists on origin).
    - `gh run list --branch 01-cross-compile-infra --limit 1 --json conclusion --jq '.[0].conclusion'` prints exactly `success`.
    - No PR has been opened: `gh pr list --head 01-cross-compile-infra --state open --json number --jq 'length'` prints `0` (REQ WORKFLOW-03 — wait for second ack).
  </acceptance_criteria>
  <done>Branch pushed to `origin/01-cross-compile-infra`; GitHub Actions reports `success` on the 5-gate matrix; no PR opened yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: User ack before PR open</name>
  <files>(none — gate)</files>
  <what-built>
    `origin/01-cross-compile-infra` carries 4 commits on top of `upstream/scala-3`, GitHub Actions CI completed green on Java 17 with all 5 gate commands passing. Ready to open a PR against `AVSystem/scala-commons:scala-3`.
  </what-built>
  <how-to-verify>
    1. Inspect the pushed branch on GitHub:
       - Open `https://github.com/&lt;your-fork&gt;/scala-commons/tree/01-cross-compile-infra`
       - Confirm green CI badge / latest run status.
       - Confirm 4 commits visible; commit messages have no GSD nomenclature.
       - Confirm diff against `AVSystem/scala-commons:scala-3` is exactly: `.scalafmt.conf`, `project/plugins.sbt`, `project/Commons.scala`, `.github/workflows/ci.yml`.
       - Confirm NO `.planning/` paths in diff.

    2. Draft PR title and body (suggested — confirm or amend):
       - Title: `Cross-compile infrastructure (jvm/jvm2 aggregate, Scala 3.8.2 baseline, made 0.1.0)`
       - Body should describe: aggregate split, per-module crossScalaVersions, scalafmt dialect inversion, sbt-mima-plugin bump, CI matrix on Java 17, made dep on Scala 3, jetty under jvm2, mkSourceDirs helper preserved from upstream.

    3. When satisfied, ack the PR open. After your ack, Claude will run:
       ```
       gh pr create --repo AVSystem/scala-commons --base scala-3 --head &lt;your-fork-owner&gt;:01-cross-compile-infra --title '...' --body '...'
       ```
       PR target: `AVSystem/scala-commons:scala-3` (REQ WORKFLOW-02). PR is opened ONLY — manual merge by maintainer per global rule (per ROADMAP "Per-PR Workflow" step 12: "Maintainer merges manually — Claude never merges").
  </how-to-verify>
    <action>Pause execution. Present the verification steps above to the user, surface the GitHub Actions run URL, and wait for the user's reply. Do NOT open the PR until the user responds with `ack pr` (with optional amended title/body).</action>
  <verify><automated>echo 'checkpoint — requires human ack'</automated></verify>
  <done>User has replied with `ack pr`. Title and body locked in for Task 7.</done>
  <resume-signal>Reply `ack pr` (with optional title/body text or amendments) to authorize `gh pr create`. Reply with issue list to block.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Open PR against AVSystem/scala-commons:scala-3 (after Task 6 ack)</name>
  <files>(none — gh API call)</files>
  <read_first>(none)</read_first>
  <action>
    Only run after Task 6 ack. The user may amend the title/body during the ack — use their values exactly; otherwise use the defaults below.

    Default title: `Cross-compile infrastructure (jvm/jvm2 aggregate, Scala 3.8.2 baseline, made 0.1.0)`

    Default body (multi-line via HEREDOC):

        Cross-compile infrastructure slice for the Scala 3 migration. No source ports — only build infra.

        ## Changes

        - **build.sbt-level**: jvm aggregate (cross-built modules) + jvm2 aggregate (jetty, 2.13-only) + existing js aggregate. analyzer/spring remain unaggregated 2.13-only orphans (formalization in a later PR).
        - **Per-module `crossScalaVersions := Seq(scala3Version, scala2Version)`** on macros, core, mongo, hocon, core-js, mongo-js, benchmark, benchmark-js, comprof. `crossScalaVersions := Seq(scala2Version)` on jetty with full skip block (`update/Compile/Test/publish` skipped on Scala 3).
        - **`madeVersion := "0.1.0"`** (pinned release on Sonatype Central). Wired as conditional dep on `core` for Scala 3 only.
        - **scalafmt dialect inversion**: default `runner.dialect = scala3` + `fileOverride` pinning `scala-2.13/` and `scala-2/` paths to `scala213source3` (required for `-Xsource:3` parsing).
        - **`sbt-mima-plugin` 1.1.4 -> 1.1.5**. `sbt-tasty-mima` deferred (no Scala 3 baseline yet).
        - **CI**: Java 17 only (was 17/21/25). Single `WorkflowStep.Sbt` running 5 gate commands: `+jvm/test`, `+jvm2/test`, `+js/test`, `++2.13 mimaReportBinaryIssues`, `scalafmtCheckAll`. Replaces previously separate `mima` (Java 21) and `scalafmt` (Java 21) jobs.

        ## What is NOT in this PR

        - No `.scala` source ports.
        - No MIGRATION.md (lands in the next PR).
        - No `macros` Scala 3 stub yet (separate PR).
        - No `made` integration code (separate PR).
        - `sbt-tasty-mima` deferred to a later PR.
        - Java 21/25 / LTS-vs-current decision deferred to first `_3` release.

        ## Verification

        - All 5 gate commands green locally on Java 17.
        - Green CI run on this branch: see Actions tab.
        - `++3.8.2 jvm/compile` green (empty jars on Scala 3 side OK — modules have no Scala 3 sources yet).

    Run:

        cd /Users/bkozak/IdeaProjects/scala-commons3
        gh pr create \
          --repo AVSystem/scala-commons \
          --base scala-3 \
          --head &lt;your-fork-owner&gt;:01-cross-compile-infra \
          --title 'Cross-compile infrastructure (jvm/jvm2 aggregate, Scala 3.8.2 baseline, made 0.1.0)' \
          --body "$(cat &lt;&lt;'PR_BODY_EOF'
        ...body content above...
        PR_BODY_EOF
        )"

    Determine `&lt;your-fork-owner&gt;` from `git remote get-url origin` (parse the GitHub owner segment).

    DO NOT auto-merge. Per global rule: "Maintainer merges manually — Claude never merges." Per CLAUDE.md user instructions: "Never merge PRs or MRs automatically. Create the PR/MR and leave it for manual review and merge."

    Record the PR URL in the SUMMARY.
  </action>
  <verify>
    <automated>gh pr list --repo AVSystem/scala-commons --head 01-cross-compile-infra --state open --json url --jq '.[0].url' | grep -E '^https://github\\.com/AVSystem/scala-commons/pull/[0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --repo AVSystem/scala-commons --head 01-cross-compile-infra --state open --json url --jq 'length'` prints `1` (exactly one open PR).
    - The PR base is `scala-3`: `gh pr view --repo AVSystem/scala-commons $(gh pr list --repo AVSystem/scala-commons --head 01-cross-compile-infra --state open --json number --jq '.[0].number') --json baseRefName --jq '.baseRefName'` prints `scala-3`.
    - The PR title contains no GSD nomenclature: `gh pr view --repo AVSystem/scala-commons $(gh pr list --repo AVSystem/scala-commons --head 01-cross-compile-infra --state open --json number --jq '.[0].number') --json title --jq '.title' | grep -iE 'gsd|phase [0-9]|plan-phase'` exits 1.
    - The PR body contains no GSD nomenclature similarly.
    - The PR is NOT merged: `gh pr view ... --json state --jq '.state'` prints `OPEN`.
  </acceptance_criteria>
  <done>PR opened against `AVSystem/scala-commons:scala-3` with title/body confirmed by user; PR remains open for maintainer manual merge; URL recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
After Plan 03 completes:

1. `cd /Users/bkozak/IdeaProjects/scala-commons3 && git log upstream/scala-3..HEAD --oneline | wc -l` prints `4` (1 plugins + 1 scalafmt + 1 Commons.scala build-structure + 1 Commons.scala+ci.yml).
2. `cd /Users/bkozak/IdeaProjects/scala-commons3 && git diff upstream/scala-3..HEAD --name-only | sort -u` lists exactly: `.github/workflows/ci.yml`, `.scalafmt.conf`, `project/Commons.scala`, `project/plugins.sbt`.
3. No `.planning/` paths in any commit (REQ WORKFLOW-05): `git log upstream/scala-3..HEAD --name-only | grep -c '^\.planning'` prints `0`.
4. No GSD nomenclature in any commit message (REQ WORKFLOW-04): `git log upstream/scala-3..HEAD --format=%B | grep -iE 'gsd|phase [0-9]|plan-phase'` exits 1.
5. `sbt githubWorkflowCheck` exits 0 (REQ INFRA-07 enforcement).
6. CI green on origin/01-cross-compile-infra (REQ INFRA-07 effective verification).
7. Open PR exists against `AVSystem/scala-commons:scala-3` (REQ WORKFLOW-02).
8. PR base is `scala-3`, not merged (REQ WORKFLOW-03 + global no-auto-merge rule).
</verification>

<success_criteria>
- `githubWorkflowJavaVersions := Seq(JavaSpec.temurin("17"))` only (INFRA-07).
- `githubWorkflowBuild` runs the 5 gate commands in one `WorkflowStep.Sbt` (INFRA-07).
- Old `mima` and `scalafmt` added jobs removed (per user directive).
- `.github/workflows/ci.yml` regenerated and committed (INFRA-07).
- All 5 gate commands pass locally on Java 17.
- Branch pushed to `origin` only after explicit user ack (WORKFLOW-03).
- PR opened against `AVSystem/scala-commons:scala-3` only after second explicit user ack (WORKFLOW-02, WORKFLOW-03).
- No `.planning/` paths and no GSD nomenclature anywhere in branch history (WORKFLOW-04, WORKFLOW-05).
- WORKFLOW-01 satisfied (branch was cut from `upstream/scala-3` in Plan 01).
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-compile-infrastructure/01-03-SUMMARY.md` capturing:
- Final commit list on `01-cross-compile-infra`: `git log upstream/scala-3..HEAD --oneline`
- Output of `sbt githubWorkflowCheck` (last 5 lines)
- Output of `sbt '+jvm/test' '+jvm2/test' '+js/test' '++2.13 mimaReportBinaryIssues' scalafmtCheckAll` (last 20 lines + total wall time)
- GitHub Actions run URL (from `gh run list`)
- PR URL (from `gh pr view`)
- Confirmation that no `.planning/` paths and no GSD nomenclature appear in branch history
</output>
